import { IArticleCardImage } from './ArticleCardImage';

const base: IArticleCardImage = {
  category: 'category',
  title: 'title',
  image: 'url',
};

export const mockArticleCardImageProps = {
  base,
};
